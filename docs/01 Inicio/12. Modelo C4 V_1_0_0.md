# 12. Arquitectura de software — Modelo C4

[← Volver al README Principal](../../README.md)

## Nivel 1 — Contexto

```mermaid
graph TD
  O[Operador, conductor y gerencia] -->|HTTPS| S[EcoLogística Huancayo]
  S --> M[OpenStreetMap / geocodificación]
  S --> N[Servicio de correo/notificaciones]
```

## Nivel 2 — Contenedores

```mermaid
graph TD
  B[React/Vite SPA] -->|REST JSON| A[API FastAPI]
  A --> DB[(PostgreSQL/PostGIS)]
  A --> E[Motor VRPTW/Green VRP]
  A --> C[Calculador CO2]
  A --> L[Servicio de logs/auditoría]
```

## Nivel 3 — Componentes de API

```mermaid
graph TD
  R[Router HTTP] --> AU[Middleware autenticación/RBAC]
  AU --> PS[PedidosService]
  AU --> FS[FlotaService]
  AU --> RS[RutasService]
  RS --> OP[OptimizerAdapter]
  PS --> PR[PedidoRepository]
  FS --> FR[FlotaRepository]
  RS --> RR[RutaRepository]
  PR --> DB[(PostgreSQL/PostGIS)]
  FR --> DB
  RR --> DB
```

Las interfaces entre componentes se validan mediante contratos OpenAPI y pruebas de integración.
